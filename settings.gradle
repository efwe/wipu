pluginManagement {
    repositories {
        mavenCentral()
        gradlePluginPortal()
        mavenLocal()
    }
    plugins {
        id(settings.getProperty('quarkusPluginId')) version settings.getProperty('quarkusPluginVersion')
    }
}
rootProject.name = 'wipu'
